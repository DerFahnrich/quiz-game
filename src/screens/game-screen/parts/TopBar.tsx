import React from "react";

const TopBar = (): JSX.Element => {
  return (
    <div className="top-bar">
      <span className="cancel">Ge upp</span>
      <span className="logo">QUIZ GAME</span>
    </div>
  );
};

export default TopBar;
